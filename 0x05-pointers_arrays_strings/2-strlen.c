#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen - prints the length of a given string
 * @s: its a pointer variable
 * Return: string length
 */
int _strlen(char *s)
{
	int result = strlen(s);

	return (result);
}
