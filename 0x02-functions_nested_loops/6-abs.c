#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _abs - to compute the absolute value of integer input.
 * Return: absolute value.
 * @n: it refers to an integer
 */
int _abs(int n)
{
	return (abs(n));
}
