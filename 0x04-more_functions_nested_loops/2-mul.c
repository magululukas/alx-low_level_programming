#include <stdio.h>
#include "main.h"

/**
 * mul - multiplie two ints
 * Return: multiplication result
 * @a: variable
 * @b: variabale
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}

